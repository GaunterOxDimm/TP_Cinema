<div id="carouselExampleDark" class="carousel carousel-dark slide" data-bs-ride="carousel">
        <div class="carousel-indicators">
                <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
                <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="1" aria-label="Slide 2"></button>
                <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="2" aria-label="Slide 3"></button>
        </div>
    
        <div class="carousel-inner">
       {# {% for key, film in allfilms %}
        <li>{{ key }}: {{ film.get_affiche|e }} {{ key }}: {{ film.get_annee|e }} {{ key }}: {{ film.get_nom|e }} {{ key }}: {{ film.get_prenom|e }} {{ key }}: {{ film.get_realisateur|e }} {{ key }}: {{ film.get_titre|e }}</li>
        <br>
       {% endfor %} #}
        

            
                <div class="carousel-item {% if loop.index == 1 %} active {% endif %} col-2" data-bs-interval="10000">
                       <img src="{{ film.get_affiche }}" class="d-block w-100" alt="..."> 
                        <div class="carousel-caption d-none d-md-block">
                                <h5>{{ film.get_titre }}</h5>
                                <p>Some representative placeholder content for the first slide.</p>
                        </div>
                </div>
              
       
                <div class="carousel-item col-2" data-bs-interval="2000">
                        <img src="{{ film.get_affiche }}" class="d-block w-100" alt="...">
                        <div class="carousel-caption d-none d-md-block">
                                <h5>{{ film.get_titre }}</h5>
                                <p>Some representative placeholder content for the second slide.</p>
                        </div>
                </div>
       
                <div class="carousel-item col-2">
                        <img src="{{ film.get_affiche }}" class="d-block w-100" alt="...">
                        <div class="carousel-caption d-none d-md-block">
                                <h5>{{ film.get_titre }}</h5>
                                <p>Some representative placeholder content for the third slide.</p>
                        </div>
                </div>
               
       
        </div>